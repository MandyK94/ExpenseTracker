spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/expense_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}